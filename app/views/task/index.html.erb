<h1>Associated Attributes - Task</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>General Info</th>
      <th>Task Info</th>
      <th>Product Info</th>
    </tr>
  </thead>
  <tbody>
  <% @associated_attrs.each do |attr| %>
    <% if attr.associated_endpoints.include?("Task") %>
      <tr>
        <td><%= attr.attr_title %></td>
        <td><%= attr.general_info %></td>
        <td><%= attr.task_info %></td>
        <% if attr.product_info['Task'].present? %>
          <td><%= attr.product_info['Task'] %></td>
        <% end %>
      </tr>
    <% end %>
    <% end %>
  </tbody>

  
</table>