<h1>Associated Attributes - SellerTransaction</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>General Info</th>
      <th>SellerTransaction Info</th>
      <th>Product Info</th>
    </tr>
  </thead>
  <tbody>
  <% @associated_attrs.each do |attr| %>
    <% if attr.associated_endpoints.include?("SellerTransaction") %>
      <tr>
        <td><%= attr.attr_title %></td>
        <td><%= attr.general_info %></td>
        <td><%= attr.sellertransaction_info %></td>
        <% if attr.product_info['SellerTransaction'].present? %>
          <td><%= attr.product_info['SellerTransaction'] %></td>
        <% end %>
      </tr>
    <% end %>
    <% end %>
  </tbody>

  
</table>