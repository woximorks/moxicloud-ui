<h1>Associated Attributes - Office</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>General Info</th>
      <th>Office Info</th>
      <th>Product Info</th>
    </tr>
  </thead>
  <tbody>
  <% @associated_attrs.each do |attr| %>
    <% if attr.associated_endpoints.include?("Office") %>
      <tr>
        <td><%= attr.attr_title %></td>
        <td><%= attr.general_info %></td>
        <td><%= attr.listing_info %></td>
        <% if attr.product_info['Office'].present? %>
          <td><%= attr.product_info['Office'] %></td>
        <% end %>
      </tr>
    <% end %>
    <% end %>
  </tbody>

  
</table>