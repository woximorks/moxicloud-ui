<h1>Associated Attributes - SoldListing</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>General Info</th>
      <th>SoldListing Info</th>
    </tr>
  </thead>
  <tbody>
    <% @associated_attrs.each do |attr| %>
      <% if attr.associated_endpoints.include?("SoldListing") %>
        <tr>
          <td><%= attr.attr_title %></td>
          <td><%= attr.general_info %></td>
          <td><%= attr.soldlisting_info %></td>
        </tr>
      <% end %>
    <% end %>
  </tbody>
</table>