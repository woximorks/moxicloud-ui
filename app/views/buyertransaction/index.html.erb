<h1>Associated Attributes - BuyerTransaction</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>General Info</th>
      <th>BuyerTransaction Info</th>

    </tr>
  </thead>
  <tbody>
    <% @associated_attrs.each do |attr| %>
      <% if attr.associated_endpoints.include?("BuyerTransaction") %>
        <tr>
          <td><%= attr.attr_title %></td>
          <td><%= attr.general_info %></td>
          <td><%= attr.buyertransaction_info %></td>

        </tr>
      <% end %>
    <% end %>
  </tbody>
</table>